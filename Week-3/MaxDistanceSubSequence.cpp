#include <bits/stdc++.h>
using namespace std;

// not finished

const int maxn = 100001;
int n, c, x[maxn];

int main() {
    int t;
    cin >> t;
    while (t--) {
        cin >> n >> c;
        memset(x, 0, sizeof(x));
        for (int i = 0; i < n; ++i) {
            cin >> x[i];
        }
        
    }
}